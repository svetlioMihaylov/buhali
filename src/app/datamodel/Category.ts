export class Category {
    id: number;
    name: string;

    // build(id: number, name: string) {
    //     let tmp = new Category;
    //     tmp.id = id;
    //     tmp.name = name;
    //     return tmp;
    // }
}